class Matrix:
    def __init__(self, rows, cols, val):
        self.rows = rows
        self.cols = cols
        self.matrix = []
        for i in range(0, rows):
            matrix[i] = [val] * cols

a = Matrix(2,2,0)
