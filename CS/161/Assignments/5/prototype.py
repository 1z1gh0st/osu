import math

if __name__ == "__main__":
    # The following would be command line arguments rather than user input
    players = input("How many players? : ")
    n = input("How many rows? : ")
    m = input("How many columns? : ")
    B = [[[0]*m]*n]
